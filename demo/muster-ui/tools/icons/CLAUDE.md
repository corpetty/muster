# tools/icons — inherited from upstream, unmaintained here

Upstream's icon pipeline: `make-icons.sh` rasterises the SVG sources into the
PNGs that `src/qml/ChatUi/icons/` ships.

Only needed when adding an icon. Muster reuses upstream's set (`plus.png` does
duty as the "ask for an address" action), so this has not been run in this fork.
