# doctests/group — inherited from upstream, unmaintained here

Upstream `logos-co/logos-chat-ui`'s three-instance group harness. Muster's demo
journey is 1:1, so nothing here is exercised.

Kept only so the flake's `#group` app keeps resolving. Delete both together if
the group surfaces are ever dropped from this fork.
